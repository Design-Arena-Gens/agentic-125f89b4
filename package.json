{
  "name": "vinyasa-club-platform",
  "version": "1.0.0",
  "private": true,
  "description": "Vinyasa Club Attendance & Performance Tracker – full-stack platform for member management, attendance, and analytics.",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "install:all": "npm install --workspaces",
    "dev": "npm run dev --workspace frontend",
    "dev:backend": "npm run dev --workspace backend",
    "dev:frontend": "npm run dev --workspace frontend",
    "format": "npm run format --if-present --workspaces",
    "lint": "npm run lint --if-present --workspaces",
    "prisma:generate": "npm run prisma:generate --workspace backend",
    "prisma:migrate": "npm run prisma:migrate --workspace backend",
    "seed": "npm run seed --workspace backend",
    "start": "npm run start --workspace backend"
  },
  "engines": {
    "node": ">=18"
  }
}
